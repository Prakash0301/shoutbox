events {}

http {
  upstream web {
    server web:5000;        # "web" = the app service name
  }
  server {
    listen 80;
    location / {
      proxy_pass http://web;
      proxy_set_header Host $host;
    }
  }
}
